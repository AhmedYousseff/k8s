apiVersion: v1
kind: Service
metadata:
  name: fleetman-queue
spec:
  ports:
    - name: http
      port: 8161
      nodePort: 30060
  selector:
    app: fleetman-queue
    release: "1"
  type: NodePort